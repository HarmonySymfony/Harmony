<form method="post" action="{{ path('app_medicament_delete_backoffice', {'id': medicament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicament.id) }}">
    <button type ="Delete" class="btn btn-primary btn-lg" style="background-color: #343a40;">Delete</button>
</form>
