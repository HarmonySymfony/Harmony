{% extends 'base.backoffice.html.twig' %}

{% block title %}Medicament{% endblock %}

{% block body %}
    <h1>Medicament</h1>
<div style="padding-left: 200px;padding-top: 200px" class="container">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ medicament.reference }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ medicament.stock }}</td>
            </tr>
            <tr>
                <th>Disponibilite</th>
                <td>{{ medicament.disponibilite }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ medicament.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ medicament.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medicament_index_backoffice') }}">back to list</a>

    <a href="{{ path('app_medicament_edit_backoffice', {'id': medicament.id}) }}">edit</a>

    {{ include('backoffice/medicament/_delete_form.html.twig') }}
    </div>    
{% endblock %}
