{% extends 'base.html.twig' %}

{% block title %}New Medicament{% endblock %}

{% block body %}
<div style="padding-left: 200px;padding-top: 200px" class="container">

    <h1>Create new Medicament</h1>

    {{ include('frontoffice/medicament/_form.html.twig') }}

      
    <div class="row mt-4">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-body">
                    <div class="d-flex justify-content-between">
                        <a class="btn btn-secondary" href="{{ path('app_medicament_index') }}">Back to list</a>                        
                        <!-- Ajoutez d'autres liens ici selon vos besoins -->
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
    
{% endblock %}
