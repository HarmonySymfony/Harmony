<form method="post" action="{{ path('app_laboratoires_delete', {'id': laboratoire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ laboratoire.id) }}">
    <button class="btn">Delete</button>
</form>
