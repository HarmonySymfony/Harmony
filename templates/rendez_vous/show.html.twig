{% extends 'base.backoffice.html.twig' %}

{% block body %}
    <h1>RendezVous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ rendez_vou.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ rendez_vou.prenom }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rendez_vou.date }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ rendez_vou.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendez_vous_index') }} " class="btn ">back to list</a>

    <a href="{{ path('app_rendez_vous_edit', {'id': rendez_vou.id}) }}" class="btn ">edit</a>

    {{ include('rendez_vous/_delete_form.html.twig') }}
{% endblock %}
