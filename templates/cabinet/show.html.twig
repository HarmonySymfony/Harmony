{% extends 'base.backoffice.html.twig' %}

{% block body %}
<div class="container">




    <h1>Cabinet</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ cabinet.id }}</td>
        </tr>
        <tr>
            <th>Adress</th>
            <td>{{ cabinet.adress }}</td>
        </tr>
        <tr>
            <th>Horaires</th>
            <td>{{ cabinet.horaires }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ cabinet.email }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cabinet_liste') }}" class="btn ">back to list</a>

    <a href="{{ path('app_cabinet_edit', {'id': cabinet.id}) }}" class="btn ">edit</a>

    {{ include('cabinet/_delete_form.html.twig') }}



</div>
{% endblock %}