<form method="post" action="{{ path('app_cabinet_delete', {'id': cabinet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cabinet.id) }}">
    <button class="btn">Delete</button>
</form>
